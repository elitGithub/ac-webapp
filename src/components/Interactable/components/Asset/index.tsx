// Libraries
import React from 'react';

interface AssetProps {
  asset: string;
  offset: {
    x: number;
    y: number;
  };
}

export const Asset: React.FC<AssetProps> = () => {
  return <div />;
};
